# Android Studio project

Open the `android` folder in Android Studio. The app loads the web game from `app/src/main/assets/index.html` into a `WebView`.

Notes:
- Minimum SDK is 21. Adjust in `app/build.gradle` if needed.
- You can build and run on an Android device or emulator.
